# A04

This directory contains files related to my solution of the A04 assignment.

Namely:
`db.py` - the main script
`city.csv, country.csv, countrylanguage.csv` - the data source files

## Instructions to run the script:

Clone the repository from GitHub:
`git clone https://github.com/kalinapetrcuni/ci2.git`

Navigate into the `A04` folder within the repository.

Run the `db.py` script (provide the source files as commandline arguments) in an eviroment with Python 3.13 available:
`python db.py city.csv country.csv countrylanguage.csv`